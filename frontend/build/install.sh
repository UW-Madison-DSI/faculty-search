# Install rollupjs bundler
sudo npm install rollup -g

# Install javascript compressor
sudo npm install terser -g

# Install javascript minifier
sudo npm install jsmin -g

# Install lessc compiler
sudo npm install less -g

# Install css minifier
# sudo npm install clean-css-cli -g
sudo npm install cssmin -g